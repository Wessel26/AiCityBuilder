package com.example.aicitybuilder.settlement;

/** Lifecycle states for a planned/active building inside a settlement. */
public enum BuildState {
    PLANNED,
    WAITING_RESOURCES,
    BUILDING,
    COMPLETE
}
